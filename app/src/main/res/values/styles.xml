<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorBlack</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <item name="android:navigationBarColor">@color/colorWhite</item>
        <item name="android:windowBackground" tools:ignore="DuplicateDefinition">@color/colorWhite</item>
        <item name="android:text">@color/textColorPrimary</item>
<!--        <item name="windowActionBar">false</item> &lt;!&ndash; 解決在 宣告 toolbar時的錯誤 &ndash;&gt;-->
<!--        <item name="windowNoTitle">true</item> &lt;!&ndash; 解決在 宣告 toolbar時的錯誤 &ndash;&gt;-->

    </style>

    <style name="toolbarstyletest" parent="ThemeOverlay.AppCompat.Light">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="overlapAnchor">false</item>
        <item name="android:actionOverflowButtonStyle">@style/OverflowButtonStyle</item>
    </style>
    <style name="OverflowButtonStyle" parent="Base.Widget.AppCompat.ActionButton.Overflow">
        <item name="android:src">@drawable/ic_history</item>
    </style>

</resources>